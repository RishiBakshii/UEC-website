import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      // main: '#715DF2'
      main:"#ffffff"
    },
    secondary: {
      main: '#002233',
    },
    fontColor:{
        light:"black",
        dark:"black"
    }
  },
});

export default theme;
