import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#715DF2'
    },
    secondary: {
      main: '#002233',
    },
    fontColor:{
        light:"white",
        dark:"black"
    }
  },
});

export default theme;
